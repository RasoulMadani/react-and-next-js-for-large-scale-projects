https://github.com/abbassep3200/classbon-nextjs

yarn add colord
https://github.com/omgovich/colord

pnpm add clsx

pnpm add classnames

pnpm dlx storybook@latest init
pnpm run storybook

// اضافه کردن کتابخانه های تست نویسی ری اکت
pnpm add -D @testing-library/jest-dom @testing-library/react @testing-library/user-event jest jest-environment-jsdom ts-jest
تغییر نسخه برای حل یک مشکل در تست نویسی
pnpm add -D @testing-library/jest-dom@5.16.5

pnpm test

pnpm add -D @storybook/addon-jest

pnpm test:generate-output --watch


pnpm add -D gulp gulp-rename gulp-wrap gulp-replace gulp-svgmin
cd icons
sudo npm install gulp -g

cd icons
gulp


pnpm up next react react-dom eslint-config-next @storybook/nextjs  --latest


PnZK9


cd icons
gulp

pnpm add axios


pnpm add react-query